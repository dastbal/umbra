# Architectural Decision Records — Umbra

Index of every decision record in this module. **Read this table first**, match
your task against the `Tags` column, and open only the records that match.
Reading all of them costs a lot of context and is only warranted on an explicit
deep audit.

When a record and the code disagree: the **code** is authoritative for what the
system does now, the **record** for why it was built that way. A disagreement
means the record needs an amendment — flag it instead of silently picking a side.

| ADR | Status | Tags | Decision |
|---|---|---|---|
| [001](./ADR-001-agent-orchestration-context.md) | Accepted | `orchestration`, `subagents`, `context` | Task size selects the topology: small tasks run a single agent, larger ones use one delegation level (`Supervisor → Researcher / Coder / Verifier`). |
| [002](./ADR-002-model-routing-and-bounded-analysis.md) | Accepted | `models`, `routing`, `evidence`, `cost` | Model resolution order is explicit `--model` > `AGENT_MODEL` > project profile, and orchestrator roles resolve their own profile without reading the env var. |
| [003](./ADR-003-on-demand-readme-index.md) | Accepted | `rag`, `readme`, `context-budget` | `list_readmes` builds a cached path/title index in `.agent/` instead of injecting README bodies into context. |
| [004](./ADR-004-on-demand-adr-index.md) | Accepted — amended 2026-08-28 | `adr`, `index`, `context-budget`, `parser` | `list_adrs` builds a cached ADR catalog in `.agent/` with bounded context, so decision history is consulted without reading every record. **Amended:** the parser recognised only the Spanish headings of the first four records, so 16 of 20 reported no status and no context to the agent at all. |
| [005](./ADR-005-incomplete-tool-checkpoint-recovery.md) | Accepted | `sessions`, `checkpoint`, `recovery` | Named sessions interrupted after a tool result are recovered rather than left in an unusable checkpoint. |
| [006](./ADR-006-vertex-tool-cycle-streaming-fallback.md) | Accepted | `vertex`, `streaming`, `tools`, `provider` | Tool cycles use a non-streaming Vertex transport, because the streamed path rejects completed cycles. |
| [007](./ADR-007-self-healing-tool-cycle-sessions.md) | Accepted | `sessions`, `recovery`, `resilience` | A session whose tool cycle the provider rejected resets itself instead of failing the run. |
| [008](./ADR-008-bounded-interactive-iteration-audit.md) | Accepted — amended 2× 2026-08-27/28 | `iteration-budget`, `telemetry`, `limits` | Interactive Deep-agent iterations are bounded and joined to performance telemetry. **Amended:** the orchestrated path was recorded as already covered and was not — see ADR-014; and the tool-call budget did not hold in 13 of 120 recorded turns — superseded by ADR-019. |
| [009](./ADR-009-executable-agent-security-policy.md) | Accepted — amended 2026-08-25 | `security`, `authorization`, `policy`, `tools` | `AgentSecurityPolicy` evaluates every filesystem and verification action in code, returning `allow` / `require_approval` / `deny`. **Amended:** two of its claims did not hold — see ADR-011. |
| [010](./ADR-010-umbra-public-package-and-cli.md) | Accepted — amended 2026-08-25 | `packaging`, `cli`, `identity`, `auth` | The published package is `@dastbal/umbra` with a single `umbra` binary and local ADC auth commands. **Amended:** the root command's routing and an unversioned build config. |
| [011](./ADR-011-path-containment-and-real-approval.md) | Accepted — amended 2026-08-27 | `security`, `path-traversal`, `hitl`, `approval`, `prompt-injection`, `pricing` | Path containment resolves the final component and returns the real path; `require_approval` raises the LangGraph interrupt the CLI already renders; file content is framed as untrusted data. **Amended:** the interrupt was raised and the CLI never rendered it — `streamEvents` reports no `__interrupt__` on any event. |
| [012](./ADR-012-shipped-working-guides-and-consumer-decision-records.md) | Accepted | `packaging`, `skills`, `adr`, `scaffolding`, `prompt` | `skills/*.md` ships with the package and `umbra init` scaffolds it plus `docs/adr/` into the consumer project; the agent records a decision there only when the work is durable, and this repository's own records stay unpublished. |
| [012](./ADR-012-arrow-key-selection-prompts.md) | Accepted — amended 5× 2026-08-26 | `cli`, `ux`, `tty`, `prompts`, `completion`, `line-editor`, `hitl`, `approval`, `slash-commands` | Menus are navigated with the arrow keys through an own prompt engine on Node's keypress decoder; the typed prompts stay live as the non-TTY fallback, and the approval gate fails closed on Escape and Ctrl+C. **Amended:** a CLI change is not verified until `dist/` is rebuilt; slash commands come from one registry instead of four restatements; `prompts.ts` is the facade (`select` / `multiSelect` / `confirm` / `askText`) with the readline lifetime enforced in one place; Tab completes from that same registry; and `line-editor.ts` now replaces `readline` at the chat prompt to show the live `/` palette, guarded by a `UMBRA_SIMPLE_PROMPT=1` escape hatch — which exposed that a lone ESC byte emits no keypress event without an owning `Interface`. The fifth amendment corrects the palette repaint origin so navigation does not move into earlier terminal output. |
| [012](./ADR-012-cli-wait-indicator-and-transient-line-contract.md) | Accepted | `cli`, `rendering`, `terminal`, `ux` | The wait indicator is a phrase with a highlight sweeping across it, and every transient line declares its printable width so it can be erased exactly and never wraps. |
| [013](./ADR-013-subagent-tool-exclusion-and-provider-diagnostics.md) | Accepted | `orchestration`, `subagents`, `provider`, `observability`, `diagnostics`, `prompt` | The delegation tool is hidden only from an agent with no subagents, so `orchestrate` can delegate again; trace batches are flushed before exit; a rejected provider request is captured redacted, credential excluded; and a contract test stops the deep prompt naming a tool the model cannot call. |
| [014](./ADR-014-delegation-mandate-shared-budget-and-question-channel.md) | Accepted — amended 2× 2026-08-27 | `orchestration`, `subagents`, `delegation`, `budget`, `hitl`, `context`, `contracts` | A delegation carries a complete order (the user request verbatim, scope, and what is already known), draws from one budget shared by the whole turn, and can ask about that order instead of guessing; a delegate that produced no result no longer spends its role's attempt. **Amended:** the mandate works in a live run; the second amendment corrects the first — a subagent *can* suspend, and the CLI was rendering no suspension at all. |
| [015](./ADR-015-claude-partner-models-through-vertex-ai.md) | Accepted | `models`, `routing`, `vertex`, `anthropic`, `claude`, `cost`, `cli` | Claude Haiku 4.5, Sonnet 5, and Opus 5 use an explicit `vertex-anthropic:` route, Google ADC and project billing, an Anthropic harness profile, and a `/model` gate that persists the required project with the model. |
| [016](./ADR-016-one-reasoning-vocabulary-across-providers.md) | Accepted | `models`, `reasoning`, `effort`, `thinking`, `cli`, `cost`, `provider` | Reasoning is one vocabulary over four provider mechanisms; the picklist appears when the model is chosen and offers only that model levels, so an invalid level cannot be saved. |
| [017](./ADR-017-prerequisites-resolved-not-guessed.md) | Accepted — amended 2026-08-28 | `provider`, `auth`, `vertex`, `ollama`, `rag`, `packaging`, `observability` | The project id is read from what the login wrote instead of guessed; Ollama declares a harness hint deepagents can resolve, so a local model reads the real disk; a partial index reports gaps; `umbra init` ignores its own state and only its own state. **Amended:** an ignore rule is powerless over a file git already tracks, so already-committed agent state is now detected and reported. |
| [018](./ADR-018-one-named-workspace-directory.md) | Accepted | `workspace`, `packaging`, `naming`, `migration`, `config` | The per-project workspace is `.umbra/`, named in exactly one constant instead of thirteen call sites, and an existing `.agent/` is moved rather than abandoned. |
| [019](./ADR-019-turn-cost-is-the-bound-not-tool-calls.md) | Accepted | `iteration-budget`, `cost`, `tokens`, `telemetry`, `limits`, `middleware` | A turn is bounded by tokens, wall clock and cost — not by tool calls, which were 1.4% of recorded elapsed time. Enforcement moves into `wrapToolCall` so the budget is a ceiling instead of a floor, the count becomes self-observed, and `limits.maxCostUsd` is enforced for the first time since it was declared. Supersedes ADR-008's tool-call budget. |
| [020](./ADR-020-a-message-that-asks-for-nothing-costs-nothing.md) | Accepted | `prompt`, `cost`, `cli`, `classifier`, `ux` | A greeting, a thank-you or a farewell is answered locally with zero model calls; affirmations such as "dale" are deliberately excluded because they mean *proceed*. The system prompt stops ordering a skill-directory listing on every unmatched message, and the conversation gate is composed above the evidence protocol. |
| [021](./ADR-021-opt-in-langsmith-onboarding.md) | Accepted | `langsmith`, `observability`, `cli`, `credentials` | LangSmith tracing is explained and enabled only by an explicit local setup flow; its credential stays in ignored `.umbra` state. |

> **Workspace directory renamed 2026-08-28.** Every `.agent/` path in records 001–017 now reads `.umbra/`. Those records are deliberately **not** rewritten — each was accurate when written — and [ADR-018](./ADR-018-one-named-workspace-directory.md) supersedes the name for all of them.


> **Numbering:** three records share the number **012** — they were written in parallel sessions. `list_adrs` reads all three (verified), so nothing is hidden from the agent, but "ADR-012" is ambiguous in prose: cite the filename. Two of the three had also been dropped from this table when the third was added; restored 2026-08-26. Renumbering is deliberately not done here — it would rewrite cross-references in already-published records.
